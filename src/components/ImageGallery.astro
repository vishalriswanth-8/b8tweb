---
const images = [
  { src: "/images/pcb-design.jpg", title: "Custom PCB Design" },
  { src: "/images/pcb-manufacturing.jpg", title: "Manufacturing Process" },
  { src: "/images/research.jpg", title: "Research & Development" }
];
---
<div class="max-w-7xl mx-auto px-4 py-8">
  <h2 class="text-2xl font-bold mb-6 text-center">Our Work Gallery</h2>
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    {images.map(image => (
      <div class="relative group overflow-hidden rounded-lg shadow-lg">
        <img 
          src={image.src} 
          alt={image.title}
          class="w-full h-64 object-cover transform transition-transform duration-300 group-hover:scale-110"
        />
        <div class="absolute inset-0 bg-black bg-opacity-40 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-center justify-center">
          <span class="text-white text-lg font-semibold">{image.title}</span>
        </div>
      </div>
    ))}
  </div>
</div>